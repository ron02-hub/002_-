# EV走行音アンケートアプリケーション 設計書

## 1. アンケート設計コンセプト

### 1.1 設計思想

#### "対話型深層探索" アプローチ
従来のアンケートは一方向的な質問→回答の繰り返しですが、本アプリケーションでは**熟練インタビュアーとの対話**を模したインタラクティブな体験を提供します。

```
┌─────────────────────────────────────────────────────────────────┐
│                  設計コンセプト：3層構造アプローチ              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │     Layer 1: 表層的印象（Surface Impression）           │   │
│   │     → SD法、リッカート尺度による定量評価               │   │
│   └─────────────────────────────────────────────────────────┘   │
│                            ↓                                    │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │     Layer 2: 認知構造（Cognitive Structure）            │   │
│   │     → 評価グリッド法によるコンストラクト抽出           │   │
│   └─────────────────────────────────────────────────────────┘   │
│                            ↓                                    │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │     Layer 3: 深層価値（Deep Values）                    │   │
│   │     → デプスインタビュー型対話による価値探索           │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 ユーザージャーニー設計

```
時間軸 ──────────────────────────────────────────────────────────────→
0分                15分               30分               45分

┌────────────┬──────────────────┬─────────────────┬────────────────┐
│  導入      │   定量評価       │   構造探索      │   深層探索     │
│  (5分)     │   (15分)         │   (12分)        │   (13分)       │
├────────────┼──────────────────┼─────────────────┼────────────────┤
│・ウェルカム│・音声聴取A       │・トライアド1   │・チャット対話  │
│・同意取得  │・SD法評価A       │・コンストラクト│・ラダリング    │
│・属性質問  │・音声聴取B       │・トライアド2   │・理想像探索    │
│・ヘッドホン│・SD法評価B       │・コンストラクト│・購買シナリオ  │
│ チェック   │・音声聴取C       │・トライアド3   │・総合評価      │
│            │・SD法評価C       │・コンストラクト│・自由記述      │
│            │・購買意欲評価    │                │                │
└────────────┴──────────────────┴─────────────────┴────────────────┘

感情曲線:
  興味    ─┐        ┌──────┐         ┌────────┐
           │        │      │         │        │
  中立    ─┼────────┘      └─────────┘        └────────→
           │   
  疲労    ─┴─────────────────────────────────────────→
          
          ※ 変化と休息のリズムを設計し、45分間の集中を維持
```

---

## 2. 評価手法詳細設計

### 2.1 SD法（Semantic Differential）評価設計

#### 評価項目の理論的背景

| カテゴリ | 評価軸 | 左極 | 右極 | 理論根拠 |
|----------|--------|------|------|----------|
| **感覚評価** | 音量感 | うるさい(-3) | 静か(+3) | 聴覚心理学 |
| | 質感 | ざらざら(-3) | 滑らか(+3) | 音質評価 |
| **感情評価** | 快感情 | 不快(-3) | 心地よい(+3) | 情動モデル |
| | 覚醒 | 退屈(-3) | ワクワク(+3) | Russell円環モデル |
| **認知評価** | 高級感 | 安っぽい(-3) | 高級感(+3) | ブランド知覚 |
| | 先進性 | 古臭い(-3) | 先進的(+3) | 技術受容 |
| **機能評価** | パワー感 | 弱々しい(-3) | 力強い(+3) | 車両評価 |
| | 安心感 | 不安(-3) | 安心(+3) | 安全性知覚 |

#### UI設計

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│   この走行音を聞いて、どのような印象を受けましたか？           │
│                                                                 │
│   うるさい ○───○───○───●───○───○───○ 静か                    │
│            -3  -2  -1   0  +1  +2  +3                          │
│                                                                 │
│   不快な  ○───○───○───○───●───○───○ 心地よい                 │
│            -3  -2  -1   0  +1  +2  +3                          │
│                                                                 │
│   ︙                                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 評価グリッド法（Repertory Grid）詳細設計

#### トライアド比較のアルゴリズム

```python
# 音声サンプルからトライアドを生成するロジック
def generate_triads(audio_samples: list, n_triads: int = 3) -> list:
    """
    バランスの取れたトライアド（3音組み合わせ）を生成
    - 各音声が均等に出現するよう調整
    - 連続で同じ音声が出ないよう制約
    """
    from itertools import combinations
    import random
    
    all_triads = list(combinations(audio_samples, 3))
    random.shuffle(all_triads)
    
    # 出現回数のバランスを考慮して選択
    selected = []
    appearance_count = {s: 0 for s in audio_samples}
    
    for triad in all_triads:
        if len(selected) >= n_triads:
            break
        # バランスチェック
        if all(appearance_count[s] < 2 for s in triad):
            selected.append(triad)
            for s in triad:
                appearance_count[s] += 1
    
    return selected
```

#### コンストラクト抽出UI

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  【ステップ1】3つの走行音を聴き比べてください                  │
│                                                                 │
│   ┌─────────┐   ┌─────────┐   ┌─────────┐                      │
│   │  音声A  │   │  音声B  │   │  音声C  │                      │
│   │  ▶ ■   │   │  ▶ ■   │   │  ▶ ■   │                      │
│   │  00:08  │   │  00:10  │   │  00:07  │                      │
│   └─────────┘   └─────────┘   └─────────┘                      │
│                                                                 │
│  【ステップ2】2つが似ていて、1つが違うとしたら？               │
│                                                                 │
│   似ている2つ: [ A ] と [ B ]  ←→  違う1つ: [ C ]             │
│                                                                 │
│  【ステップ3】似ている点は何ですか？                           │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │ 例：両方とも低音が響いていて重厚感がある                │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  【ステップ4】違う点は何ですか？                               │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │ 例：Cは高音が目立って軽快な印象                        │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

#### ラダリングプロセス

```
        上位概念探索（Why Ladder）
              ↑
    「なぜそれが重要ですか？」
    「それが得られるとどんな気持ちになりますか？」
              ↑
┌────────────────────────────────────────┐
│     抽出されたコンストラクト          │
│     例：「低音が響いて重厚感がある」  │
└────────────────────────────────────────┘
              ↓
    「具体的にはどういう音ですか？」
    「他に似た音の例はありますか？」
              ↓
        下位概念探索（How Ladder）
```

### 2.3 デプスインタビュー型対話システム設計

#### 対話フロー設計

```yaml
interview_flow:
  opening:
    - message: "ここまでの評価、お疲れ様でした。最後に、もう少し詳しくお話を聞かせてください。"
    - delay: 1500ms
    - message: "先ほど聴いていただいた走行音の中で、最も印象に残った音はどれでしたか？"
    
  topic_1_favorite:
    trigger: user_selects_audio
    questions:
      - "その音のどこが印象に残りましたか？"
      - "その印象は、ポジティブでしたか、ネガティブでしたか？"
      - follow_up:
          if_positive: "具体的に、どんな良さを感じましたか？"
          if_negative: "具体的に、何が気になりましたか？"
      - "その{feeling}は、EVを選ぶときに重要だと思いますか？"
      - "なぜ{importance_reason}だと思いますか？"
      
  topic_2_purchase_scenario:
    - "もし新しいEVを購入するとしたら、走行音はどのくらい重要ですか？"
    - "1から10で表すと、どのくらいの重要度ですか？"
    - "他の要素（価格、航続距離など）と比べるとどうですか？"
    
  topic_3_ideal_sound:
    - "あなたにとって理想的なEV走行音とは、どんな音だと思いますか？"
    - "既存の車やその他の音で、イメージに近いものはありますか？"
    
  closing:
    - "貴重なご意見をありがとうございました。最後に、全体を通して追加で伝えたいことはありますか？"
```

#### AI動的質問生成ロジック

```typescript
interface InterviewContext {
  previousAnswers: Answer[];
  currentTopic: string;
  emotionalValence: 'positive' | 'negative' | 'neutral';
  depthLevel: number; // 0-3
}

function generateFollowUpQuestion(context: InterviewContext): string {
  const templates = {
    clarification: [
      "「{keyword}」について、もう少し詳しく教えていただけますか？",
      "「{keyword}」とはどういう意味ですか？",
    ],
    why: [
      "なぜそのように感じたのですか？",
      "それが重要だと思う理由は何ですか？",
    ],
    comparison: [
      "他の音と比べて、どこが違いましたか？",
      "今まで聴いた車の音と比べるとどうですか？",
    ],
    consequence: [
      "もしその音の車を買ったら、どんな気持ちで運転できそうですか？",
      "その音が聞こえたら、周りの人はどう思うと思いますか？",
    ],
  };
  
  // 深度レベルに応じて質問タイプを選択
  if (context.depthLevel < 1) {
    return selectTemplate(templates.clarification, context);
  } else if (context.depthLevel < 2) {
    return selectTemplate(templates.why, context);
  } else {
    return selectTemplate(templates.consequence, context);
  }
}
```

---

## 3. 価値ツリー可視化設計

### 3.1 階層モデル

```
                    ┌─────────────────┐
                    │   終極価値      │
                    │ (Ultimate Values)│
                    └────────┬────────┘
                             │
         ┌───────────────────┼───────────────────┐
         ▼                   ▼                   ▼
  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
  │ 自己実現     │   │ 安心・安全   │   │ 社会的評価   │
  │ (満足感)     │   │ (安らぎ)     │   │ (ステータス) │
  └──────┬───────┘   └──────┬───────┘   └──────┬───────┘
         │                   │                   │
    ┌────┴────┐         ┌────┴────┐         ┌────┴────┐
    ▼         ▼         ▼         ▼         ▼         ▼
┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐
│先進的  ││環境配慮││信頼感  ││静粛性  ││高級感  ││洗練    │
└───┬────┘└───┬────┘└───┬────┘└───┬────┘└───┬────┘└───┬────┘
    │         │         │         │         │         │
    ▼         ▼         ▼         ▼         ▼         ▼
┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐┌────────┐
│モーター││電動    ││安定感  ││低騒音  ││重厚な  ││滑らかな│
│音が新鮮││感      ││のある音││        ││低音    ││音      │
└────────┘└────────┘└────────┘└────────┘└────────┘└────────┘
   物理的属性・具体的特徴
```

### 3.2 D3.jsによる可視化コンポーネント

```typescript
interface ValueNode {
  id: string;
  label: string;
  level: 'terminal' | 'instrumental' | 'functional' | 'physical';
  frequency: number;  // 出現頻度
  connections: string[];  // 接続ノードID
  sentiment: number;  // -1 to 1
}

interface ValueTreeConfig {
  width: number;
  height: number;
  nodeRadius: number;
  levelSpacing: number;
  transitionDuration: number;
}

// 可視化仕様
const visualSpec = {
  layout: 'hierarchical', // tree, force, radial
  direction: 'top-to-bottom',
  nodeStyle: {
    terminal: { color: '#4CAF50', size: 40 },
    instrumental: { color: '#2196F3', size: 35 },
    functional: { color: '#FF9800', size: 30 },
    physical: { color: '#9E9E9E', size: 25 },
  },
  edgeStyle: {
    type: 'curved',
    width: (freq: number) => Math.max(1, freq / 5),
    opacity: 0.6,
  },
  interaction: {
    zoom: true,
    pan: true,
    nodeClick: 'expand',
    nodeHover: 'highlight-path',
  },
};
```

---

## 4. A/Bテスト・ランダム化設計

### 4.1 実験デザイン

```
┌─────────────────────────────────────────────────────────────────┐
│                    実験デザイン概要                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ランダム割り当て                                               │
│         │                                                       │
│    ┌────┴────┐                                                  │
│    ▼         ▼                                                  │
│ ┌──────┐ ┌──────┐                                               │
│ │Group │ │Group │                                               │
│ │  A   │ │  B   │                                               │
│ └──┬───┘ └──┬───┘                                               │
│    │        │                                                   │
│    ▼        ▼                                                   │
│ 音声順序   音声順序                                             │
│ 1→2→3    3→2→1                                                  │
│    │        │                                                   │
│    ▼        ▼                                                   │
│ 評価収集   評価収集                                             │
│    │        │                                                   │
│    └────┬───┘                                                   │
│         ▼                                                       │
│    順序効果の                                                   │
│    統計検定                                                     │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 ランダム化アルゴリズム

```typescript
interface ExperimentConfig {
  groups: string[];
  factors: Factor[];
  blockingVariables: string[];  // 層化に使用する変数
}

interface Factor {
  name: string;
  levels: string[];
  type: 'between' | 'within';
}

class ExperimentAssigner {
  private config: ExperimentConfig;
  private assignments: Map<string, string>;
  
  constructor(config: ExperimentConfig) {
    this.config = config;
    this.assignments = new Map();
  }
  
  assignParticipant(participantId: string, blockingValues: Record<string, string>): string {
    // 層化ランダム割り当て
    const stratum = this.getStratum(blockingValues);
    const currentCounts = this.getStratumCounts(stratum);
    
    // 最少グループに割り当て（バランス維持）
    const minGroup = this.findMinGroup(currentCounts);
    this.assignments.set(participantId, minGroup);
    
    return minGroup;
  }
  
  getAudioOrder(group: string): string[] {
    const orders = {
      'A': ['audio_1', 'audio_2', 'audio_3'],
      'B': ['audio_3', 'audio_2', 'audio_1'],
      'C': ['audio_2', 'audio_3', 'audio_1'],
      // Latin Square設計
    };
    return orders[group];
  }
}
```

---

## 5. 画面遷移設計

### 5.1 画面フロー

```
                    ┌─────────────┐
                    │   Landing   │
                    │   Page      │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │   Consent   │
                    │   Form      │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ Demographics│
                    │   Survey    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ Audio Check │
                    │   Screen    │
                    └──────┬──────┘
                           │
          ┌────────────────┼────────────────┐
          ▼                ▼                ▼
   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐
   │ Audio Eval  │  │ Audio Eval  │  │ Audio Eval  │
   │  Sample 1   │  │  Sample 2   │  │  Sample 3   │
   └──────┬──────┘  └──────┬──────┘  └──────┬──────┘
          └────────────────┼────────────────┘
                           │
                    ┌──────▼──────┐
                    │  Repertory  │
                    │    Grid     │◄──┐
                    └──────┬──────┘   │
                           │          │ ×3回
                    ┌──────▼──────┐   │
                    │  Laddering  │───┘
                    │   Screen    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │   Depth     │
                    │ Interview   │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │   Summary   │
                    │   Screen    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  Thank You  │
                    │    Page     │
                    └─────────────┘
```

### 5.2 主要画面ワイヤーフレーム

#### デプスインタビュー画面

```
┌─────────────────────────────────────────────────────────────────┐
│  ← 戻る                    進捗: ████████████░░░ 75%           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  🎧 現在の話題: 最も印象的だった走行音について         │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  💬 インタビュアー                           14:30      │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │ 先ほど「高級感がある」とおっしゃっていましたが、 │   │   │
│  │  │ 具体的にどのような点で高級感を感じましたか？    │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │                              あなたの回答 👤 14:31      │   │
│  │    ┌──────────────────────────────────────────────┐     │   │
│  │    │ 低音がしっかり響いていて、ドイツ車みたいな   │     │   │
│  │    │ 重厚感があったからです                       │     │   │
│  │    └──────────────────────────────────────────────┘     │   │
│  │                                                         │   │
│  │  💬 インタビュアー                           14:31      │   │
│  │  ┌──────────────────────────────────────────────────┐   │   │
│  │  │ なるほど、ドイツ車のような重厚感ですね。       │   │   │
│  │  │ その重厚感があると、どんな気持ちになりますか？  │   │   │
│  │  └──────────────────────────────────────────────────┘   │   │
│  │                                                         │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  あなたの回答を入力...                                 │   │
│  │                                              [送信 ▶] │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  💡 ヒント: 具体的なエピソードや例を交えて答えると、         │
│     より深い洞察につながります                                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 6. データフロー設計

```
┌─────────────────────────────────────────────────────────────────┐
│                        データフロー図                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐     ┌──────────────┐     ┌──────────────┐        │
│  │ 回答者   │────▶│ Web Frontend │────▶│ API Server   │        │
│  │ (Browser)│◀────│   (Next.js)  │◀────│  (API Routes)│        │
│  └──────────┘     └──────────────┘     └──────┬───────┘        │
│                                                │                │
│                          ┌─────────────────────┼─────────┐      │
│                          ▼                     ▼         ▼      │
│                   ┌────────────┐       ┌────────┐  ┌─────────┐ │
│                   │ PostgreSQL │       │  S3    │  │ Redis   │ │
│                   │ (Data)     │       │ (Audio)│  │ (Cache) │ │
│                   └─────┬──────┘       └────────┘  └─────────┘ │
│                         │                                       │
│                         ▼                                       │
│                   ┌────────────────────────────────────┐        │
│                   │        分析パイプライン            │        │
│                   ├──────────┬───────────┬─────────────┤        │
│                   │ NLP処理  │ 統計解析  │ 可視化生成  │        │
│                   │ (Python) │ (Python)  │ (D3.js)     │        │
│                   └──────────┴───────────┴─────────────┘        │
│                                     │                           │
│                                     ▼                           │
│                          ┌───────────────────┐                  │
│                          │  分析ダッシュボード│                  │
│                          │  (管理者向け)     │                  │
│                          └───────────────────┘                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. コンポーネント設計

### 7.1 フロントエンドコンポーネント階層

```
App
├── Layout
│   ├── Header (進捗バー)
│   └── Footer (ナビゲーション)
│
├── Pages
│   ├── LandingPage
│   ├── ConsentPage
│   ├── DemographicsPage
│   ├── AudioCheckPage
│   ├── AudioEvaluationPage
│   │   ├── AudioPlayer
│   │   ├── SDScaleForm
│   │   └── PurchaseIntentForm
│   ├── RepertoryGridPage
│   │   ├── TriadPlayer
│   │   ├── PairSelector
│   │   └── ConstructInput
│   ├── LadderingPage
│   │   ├── ConstructCard
│   │   ├── LadderUpInput
│   │   └── LadderDownInput
│   ├── DepthInterviewPage
│   │   ├── ChatInterface
│   │   ├── MessageBubble
│   │   └── AudioReference
│   └── CompletionPage
│
└── Shared
    ├── AudioPlayer
    ├── ProgressIndicator
    ├── ScaleInput
    ├── TextInput
    └── NavigationButtons
```

---

## 更新履歴

| 日付 | バージョン | 更新内容 | 担当者 |
|------|-----------|----------|--------|
| 2026-01-01 | 1.0.0 | 初版作成 | - |

